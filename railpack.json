{
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan db:seed --class=TagSeeder --force && php -d upload_max_filesize=10M -d post_max_size=12M -d memory_limit=256M artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
