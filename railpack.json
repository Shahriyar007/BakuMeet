{
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan db:seed --class=TagSeeder --force && php artisan db:seed --class=DemoDataSeeder --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
